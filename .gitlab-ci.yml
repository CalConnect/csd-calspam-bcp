image:
  name: ribose/metanorma
  entrypoint: [""]

cache:
  paths:

build:
  stage: build
  script:
    - make clean all
    - mkdir -p public && mv *.doc *.pdf *.xml *.html public/
  artifacts:
    paths:
      - public
